# 距「初步完成」还差多少（2026-09-16 盘点）

> 之前几份文档的「待办」散落各处、部分已过期。
> 这一份是【重新核对过源码与产物】的现状。

---

# 一、已经完成的（有证据）

| 项 | 证据 |
|---|---|
| 品牌名 = Kokoa | 产物 brand.ftl：5 项全是 Kokoa |
| 4 个 AI 模块进包 | 产物 `modules/zen/Kokoa*.mjs`（构建 35032271818 核对） |
| 关于对话框无 Zen 残留 | 产物 aboutDialog.xhtml 里 zen 出现 **0** 次 |
| 设置页 Kokoa 分类 | 产物 41 个 ftl + kokoa-settings.js |
| AI 面板 / 分屏 / 会话绑定 | 源码已实现 |
| 菜单可配置（默认隐藏 3 项） | 源码已实现（待构建验证） |
| 欢迎页大标题 / 新标签页 logo | 源码已改（**在跑的构建里**，待验证） |
| 94 个单测 | 5 个测试文件，全过，每个都双向验证过 |

# 二、★ 还差什么才能算「初步完成」

## 2.1 【必须】等这次构建（35047911545）验证

```
· 欢迎页大标题删掉了吗
· 新标签页 hideLogo 生效了吗
· 菜单默认隐藏生效了吗（pref 默认值进包了吗）
· 前一轮的 dsh 正则修复
```

**这是唯一的阻塞项** —— 构建在跑（Build 阶段）。
出来后按 `docs/artifact-verification.md` 核对（先看 headSha！）。

## 2.2 【必须】实机点一遍（构建出后）

```
· 点「AI 工作区」-> dsh 起来 -> 面板打开
· 点「分屏」-> 与网页并排
· 设置页 -> Kokoa 分类能展开、勾选项能改
· 菜单 -> 打印/登录 默认不见了
```

**这些单测覆盖不了**（要真进程 / 真 UI）。

## 2.3 【没做】AI 工作区侧栏

`docs/workitem-ai-workspace-sidebar.md` 有完整设计，
但 `src/zen/kokoa/` 下【没有任何 sidebar 实现】。

**注意**：侧栏是【增强】，不是 AI 工作区的必要条件 ——
现在用「标签页 + 分屏」已经能用。建议放到初步完成【之后】。

# 三、不是阻塞、可以后做的

| 项 | 说明 |
|---|---|
| branding 图标替换 | 现在还是 Zen/Firefox 图标。等有真实截图再统一做 |
| release 流水线里的 `zen-browser/*` 引用 | 真要做发布时才需要 |
| dsh 会话切换 | 接口还没查清（`workitem-ai-panel-interface.md`）。现在【故意只记录状态】 |
| `RemotingName=kokoa-twilight` | 内部标识，不影响使用 |

---

# 四、一句话

**阻塞项只有一个：这次构建 + 实机点一遍。**

构建出来并验证通过后，AI 工作区这条主线就算【初步完成】；
侧栏、图标、会话切换是之后的增强。

---

# 五、顺带修正一份过期文档

`docs/kokoa-zen-residuals.md` 第 2 节写着「关于对话框的 Zen 必须改」，
**实测产物里已经是 0 处** —— 那行已过期，别再照着做。
