# 距「初步完成」还差多少（2026-09-16 盘点）

> 之前几份文档的「待办」散落各处、部分已过期。
> 这一份是【重新核对过源码与产物】的现状。

---

# 一、已经完成的（有证据）

| 项 | 证据 |
|---|---|
| 品牌名 = Kokoa | 产物 brand.ftl：5 项全是 Kokoa |
| 6 个 Kokoa 模块进包 | 产物 `modules/zen/Kokoa*.mjs`（构建 35069527609 核对） |
| 关于对话框无 Zen 残留 | 产物 aboutDialog.xhtml 里 zen 出现 **0** 次 |
| 设置页 Kokoa 分类 | 产物 41 个 ftl + kokoa-settings.js |
| AI 面板 / 分屏 / 会话绑定 | 源码已实现（35056127083 确认进包） |
| 菜单可配置（默认隐藏 3 项） | 已实现，构建 35056127083 验证 |
| 欢迎页大标题 / 新标签页 logo | 35047911545 确认 |
| dsh 0.1.5 接口调研 | docs/dsh-0.1.5-interface.md（包名+文件+行号） |
| 会话列表客户端 | KokoaDshSessions，49 用例，产物单测全过 |
| 197 个单测 | 10 个测试文件，全过，每个都双向验证过 |

# 二、★ 还差什么才算「初步完成」（2026-09-16 更新）

> 上一版写的是「等构建 35047911545」。
> **那次已完成**（3/4 项验证通过），所以这一节重写。

## 2.1 ⏳ 唯一的阻塞项:实机点一遍（构建验证已全部闭环）

**两轮验证构建都已核对全绿**：

| 构建 | 验证内容 | 结果 |
|---|---|---|
| 35056127083 | 菜单功能（0487ff1）+ 欢迎/hideLogo | 17/17 项 + 148/148 用例（见 docs/build-35056127083-verified.md） |
| 35069527609 | 新增第 6 模块 KokoaDshSessions（07b3bc9） | **18/18 项 + 197/197 用例**（10 文件） |

后续每次改代码仍走同一条流水线（check.sh → CI 构建 → check-artifact + 产物单测），
但不再算阻塞项 —— 它是流程，不是待办。

不实机就能查的部分（下次构建后照此跑）:

```bash
# 1) 不用实机就能查的（现在是 18 项）
python scripts/check-artifact.py <产物目录>
#    预期:模块进包 6 个 + kokoa.menu.* pref 5 个 + 品牌名 5 项
#          + 欢迎页已删 + hideLogo=true

# 2) 对着产物跑单测（验证打包没改变行为）
bash scripts/verify-artifact-modules.sh <产物目录>
```

**然后照 `docs/manual-test-checklist.md` 实机点一遍**:
```
· 点「AI 工作区」-> dsh 起来 -> 面板打开（带 token）
· 再点一次 -> 复用标签，不再新开
· 点「与网页并排」-> 左右分屏
· 设置 -> Kokoa -> 菜单:勾选能改
· 三条杠菜单:打印/登录/保存页面 默认【不在】
· Ctrl+P 仍能打印（隐藏的是入口，不是能力）
```

**这一步之前，AI 工作区不能算「验证可用」** —— 单测覆盖了逻辑，
但没覆盖「dsh 能否真被拉起」和「UI 真的长什么样」。

## 2.2 已完成（有证据）

| 项 | 证据 |
|---|---|
| 品牌名 = Kokoa | 产物 brand.ftl 五项全对 |
| 6 个模块进包 | 构建 35032271818 / 35047911545 / 35069527609 均确认 |
| 关于对话框无 Zen 残留 | 产物里 zen 出现 0 次 |
| 欢迎页大标题已删 | 35047911545 确认 |
| 新标签页 hideLogo | 35047911545 确认 = true |
| 菜单可配置 | 已实现 + 25 个用例，35056127083 验证 |
| dsh 接口三未知 | 已关闭（workitem-ai-panel-interface 第五节） |
| 单测 | 197 个用例 / 10 个文件，源码树全过；产物单测 197/197（35069527609） |

# 三、之后再说（不阻塞）

| 项 | 说明 |
|---|---|
| AI 工作区侧栏（含会话列表 UI） | 未实现。KokoaDshSessions 客户端已就绪（token 交换 + session/list + 归一化），侧栏 UI 接上即可用 |
| branding 图标 | 现在还是 Zen/Firefox 图标 |
| dsh 会话切换 | **已查清：做不到**。当前会话是 dsh 页面本地状态，无 URL 路由、无外部触发通道（dsh-0.1.5-interface.md）；产品形态改为【列表展示 + 引导用户在 dsh 内切换】，强需则向 dsh 上游提 deep-link |
| release 流水线的 zen-browser/* 引用 | 真做发布时才需要 |

# 四、一句话

**代码层验证已全部闭环（两轮构建 18+17 项 / 197+148 用例全绿）；剩下的是「实机点一遍」+ AI 工作区侧栏（会话列表 UI 接入）。**
实机验收做完，AI 工作区这条主线就算初步完成。
